// TODO: figure out where this code should eventually end up

export type AlbumData = {
    spotify_release_uri: string;
    release: {
        name: string;
        artist: string;
        img: string;
    };
};
